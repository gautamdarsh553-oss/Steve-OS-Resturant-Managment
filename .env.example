PORT=8000
JWT_SECRET=change_this_to_a_random_secret

# SMTP EMAIL CONFIG (for OTP emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your.email@gmail.com
SMTP_PASS=your_16_char_app_password
SMTP_FROM_EMAIL=noreply@restaurant.com
SMTP_FROM_NAME=Steve OS

# STRIPE CONFIG (for salary payouts)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# SUPABASE CONFIG (optional — falls back to in-memory DB if empty)
SUPABASE_URL=
SUPABASE_KEY=
FRONTEND_URL=http://localhost:8000

# GROQ API KEY (for AI voice assistant)
GROQ_API_KEY=
